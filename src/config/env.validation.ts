import * as Joi from 'joi';export const envValidationSchema = Joi.object({  PORT: Joi.number().port().default(3000),  FRONTEND_ORIGIN: Joi.string().uri().required(),  DATABASE_URL: Joi.string().uri().required(),  REDIS_URL: Joi.string().uri().required(),  SOURCE_POLLING_INTERVAL_MINUTES: Joi.number()    .integer()    .min(1)    .max(1440)    .default(30),  RATE_LIMIT_PUBLIC_JOBS_TTL_MS: Joi.number()    .integer()    .min(1000)    .default(60_000),  RATE_LIMIT_PUBLIC_JOBS_MAX: Joi.number().integer().min(1).default(60),  RATE_LIMIT_PUBLIC_JOBS_LATEST_MAX: Joi.number().integer().min(1).default(30),  RATE_LIMIT_PUBLIC_JOBS_BLOCK_MS: Joi.number()    .integer()    .min(1000)    .default(60_000),});